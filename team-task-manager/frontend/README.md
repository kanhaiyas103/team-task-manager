# Team Task Manager Frontend

Next.js + TypeScript + Tailwind CSS frontend for the Team Task Manager backend APIs.

## Setup

1. Copy `.env.local.example` to `.env.local`
2. Set `NEXT_PUBLIC_API_URL`
3. Run `npm install`
4. Run `npm run dev`
